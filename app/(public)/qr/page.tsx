// QR photo submission — public, no login required
// Accessed by scanning the QR code sticker inside the bus
export default function QRPage() {
  return (
    <div>
      <p className="p-8 text-brand-blue font-bold">QR Check-In — coming soon</p>
    </div>
  );
}
